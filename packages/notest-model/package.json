{
  "name": "@butopen/notest-model",
  "version": "1.0.0",
  "types": "src/index.ts",
  "dependencies": {
  },
  "scripts": {
    "prepare": "npm run build",
    "build": "node esbuild.cjs && npm run types",
    "types": "tsc --emitDeclarationOnly --outDir dist"
  },
  "devDependencies": {
    "esbuild": "^0.14.27",
    "typescript": "^4.6.3"
  }
}
